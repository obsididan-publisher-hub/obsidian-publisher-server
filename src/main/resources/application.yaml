spring:
  application:
    name: obsidian-publisher

api:
  base:
    path: /api

obsidian:
  vault:
    path: /home/fenya/obsidian/obsidian

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics