spring:
  application:
    name: obsidian-publisher

api:
  base:
    path: /api

obsidian:
  vault:
    path: /home/fenya/obsidian/obsidian

notes:
  startNote: "index"
  htmlConverted:
    path: "${user.dir}/html"

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics