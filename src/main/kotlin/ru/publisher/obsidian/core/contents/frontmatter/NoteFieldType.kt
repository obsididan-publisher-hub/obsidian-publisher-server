package ru.publisher.obsidian.core.contents.frontmatter

/**
 * Тип значения поля заметки (для внутреннего использования)
 */
enum class NoteFieldType {
    STRING,
    NUMBER,
    BOOLEAN,
    LIST
}
